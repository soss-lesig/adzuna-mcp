/**
 * Shared helpers for Adzuna MCP tool handlers. Will export formatJob,
 * formatLocation, formatSalary, truncate, and messageForError once build step
 * 4 lands. Lives in this file (rather than alongside search-jobs.js) so the
 * same helpers can be reused by future Adzuna tools added in v0.2.0
 * (salary history, top companies, etc.). Stub pending build step 4.
 */
